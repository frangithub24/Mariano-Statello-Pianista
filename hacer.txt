HTML
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mariano Statello</title>
    <link rel="shortcut icon" href="./public/images/flavicon.png" type="image/x-icon">
    <link rel="stylesheet" href="./public/css/normalize.css">
    <link rel="stylesheet" href="./public/css/estilos.css">
    <link rel="stylesheet" href="https://unpkg.com/boxicons@latest/css/boxicons.min.css">
</head>
<body>
    <header class="hero">
        <nav class="nav container">
            <div class="nav_logo">
                <a href="/index.html">Mariano Statello</a>
            </div>
            <ul class="nav_links">
                <li><a href="/index.html" class="nav_link nav_link--active">Home</a></li>
                <li><a href="/bio.html" class="nav_link">Bio</a></li>
                <li><a href="/partituras.html" class="nav_link">Partituras</a></li>
                <li><a href="/tutoriales.html" class="nav_link">Tutoriales</a></li>
                <li><a href="/videos.html" class="nav_link">Videos</a></li>
                <li><a href="#contacto" class="nav_link">Contacto</a></li>
            </ul>

            <!-- Botón menú móvil -->
            <div class="nav_menu" id="menuToggle">
                <img src="./public/images/menu.svg" alt="Menú" class="nav_img">
            </div>
        </nav>

        <!-- Menú fullscreen móvil -->
        <div class="nav_dropdown" id="dropdownMenu">
            <ul>
                <li><a href="/index.html" class="nav_link">Home</a></li>
                <li><a href="/bio.html" class="nav_link">Bio</a></li>
                <li><a href="/partituras.html" class="nav_link">Partituras</a></li>
                <li><a href="/tutoriales.html" class="nav_link">Tutoriales</a></li>
                <li><a href="/videos.html" class="nav_link">Videos</a></li>
                <li><a href="#contacto" class="nav_link">Contacto</a></li>
            </ul>
        </div>

        <div class="overlay" id="overlay"></div>

        <section class="heroe_container">
            <h1 class="heroe_title">Mariano Statello</h1>
            <p class="heroe_paragraph">Músico, pianista y compositor</p>
        </section>
    </header>

    <!-- Audio -->
    <audio id="audioElement" src="./public/audio/cancion.mp3"></audio>

    <div class="audioControl">
        <img src="./public/images/audioPlay.svg" id="audioControlIcon" alt="Audio Control">
    </div>
</body>
<script src="./public/js/main.js"></script>
</html>

CSS (estilos.css)
/* Reset y fuentes */
body, html { margin:0; padding:0; font-family: 'Poppins', sans-serif; }
ul { list-style: none; padding: 0; margin: 0; }

/* HEADER HERO */
.hero {
    height: 100vh;
    background: linear-gradient(rgba(0,0,0,0.4), rgba(0,0,0,0.6)), url('./public/images/foto1.jpeg') center/cover no-repeat;
    color: white;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
}

/* NAV */
.nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem 2rem;
    z-index: 20;
    position: relative;
}

.nav_links {
    display: flex;
    gap: 1.5rem;
}

.nav_link {
    text-decoration: none;
    color: white;
    font-weight: 500;
    transition: color 0.3s ease;
}

.nav_link:hover,
.nav_link--active {
    color: #FFD700;
}

/* MENU ICONO MÓVIL */
.nav_menu {
    display: none;
    cursor: pointer;
}

.nav_img {
    width: 40px;
    height: 40px;
}

/* MENU FULLSCREEN */
.nav_dropdown {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0,0,0,0.95);
    display: flex;
    justify-content: center;
    align-items: center;
    transform: translateY(-100%);
    opacity: 0;
    pointer-events: none;
    transition: transform 0.5s ease, opacity 0.3s ease;
    z-index: 15;
}

.nav_dropdown.show {
    transform: translateY(0);
    opacity: 1;
    pointer-events: auto;
}

.nav_dropdown ul {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    text-align: center;
}

.nav_dropdown a {
    color: white;
    font-size: 2rem;
    text-decoration: none;
    transition: color 0.3s ease;
}

.nav_dropdown a:hover {
    color: #FFD700;
}

/* OVERLAY */
.overlay {
    position: fixed;
    top:0; left:0;
    width:100%; height:100%;
    background: rgba(0,0,0,0.5);
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.3s ease;
    z-index: 10;
}

.overlay.show {
    opacity:1;
    pointer-events:auto;
}

/* HERO TEXT */
.heroe_container {
    text-align: center;
    margin-bottom: 3rem;
}

.heroe_title {
    font-size: 3rem;
}

.heroe_paragraph {
    font-size: 1.5rem;
}

/* AUDIO CONTROL */
.audioControl {
    position: fixed;
    bottom: 2rem;
    right: 2rem;
    cursor: pointer;
    z-index: 25;
}

/* MEDIA QUERY MÓVIL */
@media (max-width: 768px) {
    .nav_links { display: none; }
    .nav_menu { display: block; }
}

JS (main.js)
document.addEventListener("DOMContentLoaded", () => {
    const menuToggle = document.getElementById('menuToggle');
    const dropdownMenu = document.getElementById('dropdownMenu');
    const menuLinks = document.querySelectorAll('.nav_dropdown a');
    const overlay = document.getElementById('overlay');

    // Audio
    const audioElement = document.getElementById('audioElement');
    const audioControlIcon = document.getElementById('audioControlIcon');
    let isPlaying = false;

    // Abrir/cerrar menú
    menuToggle.addEventListener('click', () => {
        dropdownMenu.classList.toggle('show');
        overlay.classList.toggle('show');
    });

    // Cerrar menú al tocar overlay
    overlay.addEventListener('click', () => {
        dropdownMenu.classList.remove('show');
        overlay.classList.remove('show');
    });

    // Cerrar menú al hacer clic en enlace
    menuLinks.forEach(link => {
        link.addEventListener('click', () => {
            dropdownMenu.classList.remove('show');
            overlay.classList.remove('show');
        });
    });

    // Control audio
    audioControlIcon.addEventListener('click', () => {
        if (isPlaying) {
            audioElement.pause();
            audioElement.currentTime = 0;
            audioControlIcon.src = './public/images/audioPlay.svg';
        } else {
            audioElement.play();
            audioControlIcon.src = './public/images/audioStop.svg';
        }
        isPlaying = !isPlaying;
    });
});


✅ Qué mejora este código:

Menú fullscreen para móvil, animado desde arriba.

Overlay cubriendo toda la pantalla, se cierra con clic en él.

Links del menú funcionan para cerrar menú automáticamente.

Responsive: menú desktop se mantiene horizontal, móvil se oculta detrás del botón.

Control de audio funcional, con cambio de ícono.

Código más limpio y fácil de mantener.

Si querés, puedo hacer una versión todavía más profesional, con:

Animación de slide lateral o fade de cada link al abrir el menú.

Botón “X” animado que cambia desde el ícono hamburguesa.

Menú que ocupa el 100% de pantalla sin importar la resolución.

Eso deja tu menú como de agencia profesional.